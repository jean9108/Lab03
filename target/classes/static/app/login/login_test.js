'use strict';

describe('myApp.view3 module', function() {

  beforeEach(module('myApp.view3'));

  describe('login controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view3Ctrl = $controller('LoginCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});